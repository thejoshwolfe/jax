import java.util.regex.Matcher;
import java.util.regex.Pattern;

class Fancy {
    int gimme42(String thing) {
        Pattern p = Pattern.compile("\\d+");
        Matcher m = p.matcher(thing);
        m.find();
        Integer.parseInt(m.group())
    }
}
