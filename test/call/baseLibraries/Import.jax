import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Import {
    public static int gimme42() {
        Pattern p = Pattern.compile("\\d+");
        Matcher m = p.matcher("abc42efg");
        m.find();
        Integer.parseInt(m.group())
    }
}
